#Tuples in Python
short_tiple = ("Rolf","Bob") # tuples usually have brackets around them, not necessary but

#tuple in list
tuple_in_list = [("Rolf","Bob")]

#not a tuple
not_a_tuple = "Rolf"    #is a normal string

"""we cannot append in atuple but we can in a list
but we add two tuples together"""

#adding tuples
cats = ("ginger","breadloaf")
cats = cats + ("Jenny",)
print(cats)
